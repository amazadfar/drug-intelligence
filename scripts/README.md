# Scripts

This directory will contain reproducible command-line workflows for sample
dataset construction, benchmark execution, KG export, and evaluation.

Scripts must run on legal-safe fixtures by default.
